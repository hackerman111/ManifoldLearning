{
  "status": "interrupted after first CPU measurement; incomplete paired comparison",
  "shape": [
    10000,
    1000,
    1000,
    20,
    1
  ],
  "index_init": "random",
  "outer_steps": 2,
  "solver_max_steps": 3,
  "error": "Long full protocol interrupted; bounded one-step diagnostic follows",
  "dtype": "float64",
  "rows": [
    {
      "backend": "cpu",
      "seed": 0,
      "seconds_rounded_from_stdout": 63.313,
      "paired_complete": false
    }
  ],
  "notes": "Partial JSON was overwritten by bounded rerun; only the observed rounded stdout timing is retained. This attempt is not used in speedup claims."
}
